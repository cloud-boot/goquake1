# internal/transpile

The reproducible quakeforge -> Go transpile pipeline. Runs in a Tart
debian:13 VM (or any linux/arm64 environment with Go 1.23+ and
`modernc.org/ccgo/v4`).

## Pipeline

```text
quakeforge git checkout
    |
    | (1) ./bootstrap.sh && ./configure --enable-static --disable-shared
    |     ...
    |     yields config.h, version.h, and the .c-file-set the build
    |     would otherwise feed to cc
    |
    v
strip platform-specific .c files
    |
    | (2) filter out gl_*, sdl_*, vid_x11, snd_alsa/pulse/jack, etc.
    |     keep only the soft renderer + null I/O + portable engine core
    |
    v
ccgo -DLINUX -I... <stripped-source-set>
    |
    | (3) modernc.org/ccgo/v4 produces ../engine/engine.go (and
    |     friends), pure-Go output that imports modernc.org/libc only.
    |
    v
go build ./engine/...
    |
    | (4) confirms link-clean translation. Any "undefined: X external"
    |     surfaces as a TODO in this directory.
```

## Why a separate VM

ccgo needs the host's libc headers to parse system includes. On
macOS-arm64 (the maintainer's workstation) it dies on Darwin's NEON
struct alignment in `_structs.h`. Docker on Apple Silicon hits emulation
issues (Rosetta lacks AVX2 / x86_64 gcc lacks `-m64`). A native
linux/arm64 Tart VM is the only reliable path. The same approach is
documented for go-virtio bring-up where real validation requires Tart
VMs to unlock vsock / fs / Venus-readback paths.

## Quick start (Tart)

```sh
# host
tart clone ghcr.io/cirruslabs/debian:latest goquake-build
tart run goquake-build --no-graphics &
IP=$(tart ip goquake-build)
# bring the host-side checkout into the VM via ssh+rsync, or sshfs.
rsync -a -e "sshpass -p admin ssh -o StrictHostKeyChecking=no" \
    /Users/me/.../goquake1/ admin@$IP:/work/

# in the VM (or via ssh-exec from the host)
cd /work && bash internal/transpile/run.sh
```

## Files in this directory

| File            | State   | Purpose                                                                  |
|-----------------|---------|--------------------------------------------------------------------------|
| `run.sh`        | landed  | End-to-end driver (clone quakeforge, bootstrap + configure, ccgo)        |
| `skip.txt`      | landed  | ERE patterns of `.c` files NOT to feed ccgo                              |
| `sources.txt`   | runtime | Generated by `run.sh` — exact file list ccgo sees that round             |
| `transpile.log` | runtime | Captured ccgo stderr — undefined-extern list = next work                 |
| `Dockerfile`    | TODO    | Reproducible CI image (linux/arm64 debian:13 + Go + ccgo + clang-19)     |
| `Taskfile.yaml` | TODO    | pkgx-task targets: `task transpile`, `task verify-clean`                 |

## Known obstacles surfaced during initial bring-up

These are the open work items for the Phase Q-1a sprint:

1. **quakeforge master requires C23 `#embed`.** Debian 13 trixie's
   `gcc-14.2` does not implement it; `clang-19` does. The driver
   needs `CC=clang-19` in the `./configure` env (currently the script
   relies on the default `cc`). Patch the script to detect and pick
   a `#embed`-capable compiler automatically.

2. **quakeforge `./configure` requires audio/video dev headers even
   when those backends are `--disable`d**: `libflac-dev libogg-dev
   libvorbis-dev libxext-dev libxxf86vm-dev libvulkan-dev
   libasound2-dev libjack-jackd2-dev libsamplerate0-dev
   libfontconfig-dev libharfbuzz-dev libfreetype-dev libpng-dev
   zlib1g-dev libbacktrace-dev`. Add them to the Dockerfile (next file
   to land); for ad-hoc Tart runs, `apt-get install` the lot up
   front.

3. **quakeforge has no upstream tags** (only `master` + `wip-*`
   branches). Pin a commit SHA in `QF_REF` once we have a known-good
   transpile to guard against upstream drift.

4. **engine/ output is not yet emitted.** Once 1+2+3 are in hand, the
   first successful `run.sh` invocation produces `engine/engine.go`
   (plus any `engine_X.o.go` intermediates ccgo decides to split).
   The committed engine.go file is the artifact that
   `cloud-boot/tamago-uefi`'s probe imports.

5. **Link gaps** are expected on first transpile (Sys_Error, etc.).
   They are not failures of ccgo or quakeforge; they are the natural
   set of symbols we replace with `backend/tamago/cshim/` Go-side
   implementations. The driver prints the count and writes them to
   `transpile.log`; the iteration is to either include the missing
   `.c` file in sources or add a Go stub for the symbol.
